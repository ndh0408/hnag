# Auto-provision Prometheus as the default Grafana data source on first boot.
# Mounted into the grafana container at /etc/grafana/provisioning/datasources/.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
